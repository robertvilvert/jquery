$( document ).ready(function() {
    if($('.botao').length >0){
      // Sempre retorna um array, mesmo que seja vazio, por isso nao é possivel comparar usando == (equal)
      alert ("Tem class botao na pagina");
    }
});
